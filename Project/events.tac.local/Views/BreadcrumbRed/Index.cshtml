@model IEnumerable<events.tac.local.Models.BreadcrumRedItem>

    <!-- [Breadcrumb Navigation] -->
    <ol class="breadcrumb">
        @foreach (var navitem in Model)
        {
            <li>
                <a class="@(navitem.Active ? "active" : string.Empty)" 
                   style="color:red"
                   href="@navitem.URL">
                    @navitem.Title
                </a>
            </li>
        }
    </ol>
    <!-- [/Breadcrumb Navigation] -->